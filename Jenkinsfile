pipeline {
  agent none
  stages {
    stage('Export') {
      steps {
        bat(returnStatus: true, returnStdout: true, script: 'Test', encoding: 'UTF-8')
        sh 'sdfadsfadssd'
      }
    }
  }
}